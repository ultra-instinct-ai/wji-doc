---
title: "Video tools"
description: "Trim, capture frames, regenerate a section, upscale, and create panoramas."
---

## Local tools

<Frame caption="A video node and its toolbar">
  ![Video node with a toolbar offering trim, frame capture, selective regeneration, preview switching, and director bar](/images/localtools.png)
</Frame>

{/* SHOT infinite-canvas/video-node-toolbar-01
    Screen: a selected video node showing a frame of its clip
    Must show: the complete node toolbar with every icon legible
    State: node selected, video paused on a recognisable frame
    UI language: English
    Size: >= 1200x900
    Anonymize: no real people in the clip
  */}

These run in your browser and do not cost credits.

| Tool | Node | Does |
| --- | --- | --- |
| **Trim** | Video, audio | Shortens the clip |
| **Capture frame** | Video | Takes a still, as a new image node |
| **Crop** | Image | Standard cropping |
| **Annotate** | Image | Rectangles, circles, arrows, lines, and text, with a layer panel |
| **Clarity** | Image | A sharpening filter |
| **Quick slice** | Image | Cuts an image into a grid of separate nodes |

## Selective regeneration

<Frame caption="Selective regeneration, with all four previews">
  ![Video node offering preview switching between original, segment, generated replacement, and stitched result](/images/9月1日\(42\).gif)
</Frame>

{/* SHOT infinite-canvas/selective-edit-01
    Screen: a video node after a selective regeneration has completed
    Must show: the four preview options, and the stitch control
    State: a finished selective edit, not one still running
    UI language: English
    Size: >= 1200x900
  */}

The most useful video feature on the canvas: regenerate **part** of a clip and stitch it back.

<Steps>
  <Step title="Choose a section">
    On a video node, pick the range you want to replace.
  </Step>
  <Step title="Generate a replacement">
    WJI uploads the segment and regenerates it in edit mode.
  </Step>
  <Step title="Review the four tracks">
    The node can preview the original, the segment, the generated replacement, and the stitched result.
  </Step>
  <Step title="Stitch">
    **Stitch full video** joins it back together. The result can be added to the director bar as the stitched clip, the generated segment alone, or the before-and-after arrangement.
  </Step>
</Steps>

Selective editing requires a model that supports it. The status runs through pending, running, done, and failed.

## AI video tools

| Tool | Notes |
| --- | --- |
| **Depth video** | Requires a depth model to be available |
| **Video upscale** | Creates an upscale generation node |
| **Generate panorama** | Turns an image into a 360° panorama, viewable in the panorama viewer |

If a required model is not available on your account, WJI names the missing model rather than failing quietly.

## The panorama viewer

Panorama nodes open a dedicated viewer, either by double-clicking or from the toolbar. The viewer can export the current view as a JPEG.
