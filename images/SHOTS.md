# Screenshot worklist

> Generated by `npm run docs:check`. Do not edit by hand.

**0 / 79 captured.**

## `billing/wallet-and-records`

- [ ] `images/billing/balance-card-01.png`
      Screen: the balance card on the dashboard with View details expanded
      Must show: at least three buckets with different expiry dates, so the soonest-expires-first rule has something to illustrate
      State: a mix of granted and purchased buckets
      UI language: English
      Size: >= 1000x900

## `billing/how-credits-work`

- [ ] `images/billing/cost-estimate-01.png`
      Screen: the generate button with a cost estimate
      Must show: a fixed total in one frame and, ideally as a second capture, the per-second rate a duration-based model displays instead
      State: settings that produce a non-trivial estimate
      UI language: English
      Size: >= 900x300, cropped tight

## `billing/wallet-and-records`

- [ ] `images/billing/usage-consumption-01.png`
      Screen: the Consumption tab of the usage page
      Must show: the filters and several rows, including at least one with a refund and one with a discount so those columns are populated
      State: a workspace with real generation history
      UI language: English
      Size: >= 1800x900
      Anonymize: generic workspace and operator names

## `digital-host/overview`

- [ ] `images/digital-host/program-list-01.png`
      Screen: the Digital Host program list
      Must show: the filter row, and at least three programs sitting at different steps so the badges differ
      State: presenter and scene thumbnails visible on the rows
      UI language: English
      Size: >= 1600x900
      Anonymize: no real people as presenters

## `digital-host/script-to-video`

- [ ] `images/digital-host/step-break-01.png`
      Screen: step two of the program wizard after a split
      Must show: the seconds-per-segment control, the instruction field, and five or more segment rows with their dialogue and tone
      State: after a successful split, before any editing
      UI language: English
      Size: >= 1600x1000

- [ ] `images/digital-host/step-download-01.png`
      Screen: step five with the credit report tab active and the export menu open
      Must show: all six export options, and the credit report cards with their success rates
      State: a program with some failed attempts so the success rate is not 100 percent
      UI language: English
      Size: >= 1600x1000

- [ ] `images/digital-host/step-video-01.png`
      Screen: step four of the program wizard
      Must show: two or three segment cards in different states, ideally one approved, one generated, and one missing voiceover
      State: a program partway through video generation
      UI language: English
      Size: >= 1600x1000
      Anonymize: no real people as presenters

## `getting-started/accounts-and-sign-in`

- [ ] `images/getting-started/accounts-01.png`
      Screen: the Create Account page at /auth/register
      Must show: all five fields, the Get Code button, the Register button, and the legal agreement line
      State: empty form, no validation errors
      UI language: English
      Size: >= 1200x900
      Anonymize: leave all fields empty

## `getting-started/interface-tour`

- [ ] `images/getting-started/interface-01.png`
      Screen: the top navigation bar, cropped to the header only
      Must show: logo, the Chat / Workspace / Agents / Image Creator links with Workspace active,
      and the right-hand cluster: theme switcher, language switcher, task tracker,
      notification bell, and profile avatar
      State: signed in as a normal user, so the IP/Character and Admin links are absent
      UI language: English
      Size: >= 1600x200
      Anonymize: generic avatar, no real display name

- [ ] `images/getting-started/interface-02.png`
      Screen: a workspace open on the Idea Studio app
      Must show: top app bar with back arrow, workspace name and credit badge; left panel with the
      three app icons, toolbar, and file tree; main content area; right AI chat panel
      State: a folder selected in the file tree so the content area is not empty
      UI language: English
      Size: >= 1600x900
      Anonymize: generic workspace name and folder names

## `index`

- [ ] `images/getting-started/introduction-01.png`
      Screen: a workspace with Infinite Canvas open, showing a small graph of connected nodes
      Must show: left app rail, canvas with 4-6 nodes including at least one image and one text node,
      a visible connection between two of them, top bar with the canvas name
      State: zoom near 100%, nothing selected, no panels open
      UI language: English
      Size: >= 1600x900
      Anonymize: no real user names, emails, or workspace names

## `getting-started/quickstart`

- [ ] `images/getting-started/quickstart-01.png`
      Screen: the Create workspace dialog, opened from the workspace list
      Must show: workspace name field filled with a neutral name, both primary image generation
      mode cards side by side, the "More settings" section, and both footer buttons
      State: "Open creative image generation" selected
      UI language: English
      Size: >= 1400x900
      Anonymize: use a generic workspace name such as "Demo project"

- [ ] `images/getting-started/quickstart-02.png`
      Screen: the Idea Image Generation app with a prompt typed in
      Must show: prompt field with a short neutral prompt, model selector, settings icon,
      and the generate button displaying an estimated credit cost
      State: no reference images added, batch size at default
      UI language: English
      Size: >= 1600x900
      Anonymize: no real user names or workspace names

## `idea-studio/workspace-ai-chat`

- [ ] `images/idea-studio/ai-chat-01.png`
      Screen: the AI chat panel in Idea Studio
      Must show: the mode switch, an attached idea, and a reply containing structured idea data with the save-all button visible
      State: mid-conversation
      UI language: English
      Size: >= 800x1000

## `idea-studio/file-tree-actions`

- [ ] `images/idea-studio/ai-shortcuts-01.png`
      Screen: the file tree context menu open on an idea typed as screenplay
      Must show: the four AI shortcuts together with the general actions above them
      State: opened on an idea in the story folder
      UI language: English
      Size: >= 900x900

## `idea-studio/build-with-ai`

- [ ] `images/idea-studio/build-with-ai-01.png`
      Screen: the Build with AI dialog before running
      Must show: the reference dropdown, the inspiration text area, all six output checkboxes, the shot count field, and the full auto versus enhanced choice
      State: shots ticked so the shot count field is visible
      UI language: English
      Size: >= 1400x1000

- [ ] `images/idea-studio/build-with-ai-review-01.png`
      Screen: the Build with AI results panel after generation
      Must show: several generated cards, at least one toggled off, the target folder selector, and the status badges
      State: a mix of completed and still-processing cards if possible
      UI language: English
      Size: >= 1600x1000

## `idea-studio/idea-detail-and-gallery`

- [ ] `images/idea-studio/detail-01.png`
      Screen: an idea open in detail view inside Idea Studio
      Must show: the large idea card at top, the detailed content block, the image grid with at least
      three attached images, and the collapsed image prompt / video prompt / metadata panels
      State: a character idea with a cover image set
      UI language: English
      Size: >= 1600x1000
      Anonymize: fictional character name, no real people in the images

## `idea-studio/overview`

- [ ] `images/idea-studio/folders-01.png`
      Screen: the Idea Studio left panel with the file tree expanded
      Must show: all six system folders, with two of them expanded to reveal ideas inside
      State: a populated workspace, not an empty one
      UI language: English
      Size: >= 700x1000
      Anonymize: fictional character and location names

## `idea-studio/idea-detail-and-gallery`

- [ ] `images/idea-studio/gallery-view-01.png`
      Screen: gallery view opened from an idea with several images
      Must show: the large image, the thumbnail strip, the image and idea position indicators, and the like control
      State: middle of a set so the position indicators read something like 3 of 8
      UI language: English
      Size: >= 1600x1000
      Anonymize: no real people

## `idea-studio/create-and-edit-ideas`

- [ ] `images/idea-studio/idea-form-01.png`
      Screen: the idea form open on an existing character
      Must show: every field, with a genuinely descriptive summary filled in so the guidance on the page is illustrated, and the AI generate buttons beside the prompt fields
      State: editing rather than creating, so the fields have content
      UI language: English
      Size: >= 1200x1100
      Anonymize: fictional character

## `image-generation/manage-assets`

- [ ] `images/image-generation/asset-preview-01.png`
      Screen: the asset preview dialog opened from a result
      Must show: the info panel with generation details expanded, the seed, the credits charged, both prompts, and the use-for-generation-again control
      State: an asset that actually carries generation metadata
      UI language: English
      Size: >= 1600x1000
      Anonymize: no real people, generic asset name

## `image-generation/video-generation`

- [ ] `images/image-generation/frame-slots-01.png`
      Screen: the frame slots in the video generation form
      Must show: both slots filled with visibly different images, the swap control, and each slot remove button
      State: a model that supports both frames selected
      UI language: English
      Size: >= 1000x600
      Anonymize: no real people

## `image-generation/history-and-results`

- [ ] `images/image-generation/history-entry-01.png`
      Screen: one entry in the generation history, cropped to itself
      Must show: every metadata field named in the page, especially the seed tag and the credits charged
      State: an entry with references so their thumbnails appear
      UI language: English
      Size: >= 1400x800
      Anonymize: no real people

## `image-generation/models`

- [ ] `images/image-generation/model-select-01.png`
      Screen: the video model selector open
      Must show: both filter rows, with the first-frame filter active so only models supporting it remain
      State: several models listed with avatars and one-line descriptions
      UI language: English
      Size: >= 1200x1000

## `image-generation/manage-assets`

- [ ] `images/image-generation/move-to-dialog-01.png`
      Screen: the destination picker opened from Move to
      Must show: the folder tree with datasets, one selected, and the selection count at the bottom
      State: opened on a multi-selection so the count is greater than one
      UI language: English
      Size: >= 1000x900

## `image-generation/overview`

- [ ] `images/image-generation/overview-01.png`
      Screen: the Idea Image Generation app inside a workspace
      Must show: all three columns — dataset sidebar, dataset contents grid, generation form on the
      right — plus the four mode icons at the top of the form
      State: a dataset selected with a few existing images; image mode active
      UI language: English
      Size: >= 1600x1000
      Anonymize: generic dataset names, no real people in the images

## `image-generation/prompts`

- [ ] `images/image-generation/prompt-tabs-01.png`
      Screen: the prompt box in the generation form
      Must show: both tabs, with the final prompt selected so the difference from the user prompt is obvious, and the refresh control
      State: a short user prompt that expanded into a much fuller final prompt
      UI language: English
      Size: >= 1200x600

- [ ] `images/image-generation/prompt-templates-01.png`
      Screen: the prompt template dialog
      Must show: all four category tabs, with the character category selected so the editing-by-template idea is visible
      State: several templates listed with their thumbnails
      UI language: English
      Size: >= 1400x1000

## `image-generation/reference-images`

- [ ] `images/image-generation/reference-block-01.png`
      Screen: the unified reference block in the generation form
      Must show: at least three references with different types, their names, and the add control
      State: types visibly different so the labelling is the point of the shot
      UI language: English
      Size: >= 1200x700
      Anonymize: no real people in the reference images

## `image-generation/history-and-results`

- [ ] `images/image-generation/result-actions-01.png`
      Screen: the hover action menu on one result inside the history
      Must show: the full menu, taken inside a workspace so the Save to Idea entries are present
      State: hovering a completed image result
      UI language: English
      Size: >= 900x900

## `image-generation/generation-settings`

- [ ] `images/image-generation/settings-01.png`
      Screen: the generation settings popover open over the generation form
      Must show: the aspect ratio thumbnail grid, the batch size slider, at least one weight slider,
      and the style prompt entry
      State: an image model selected that exposes reference weight
      UI language: English
      Size: >= 1200x900
      Anonymize: none needed

## `image-generation/storyboard-mode`

- [ ] `images/image-generation/storyboard-grid-01.png`
      Screen: a completed storyboard result in the history
      Must show: all nine cells clearly distinct, and the upscale menu open on one of them offering 2K and 4K
      State: shot descriptions turned on so the captions are visible
      UI language: English
      Size: >= 1400x1200
      Anonymize: no real people

## `infinite-canvas/asset-library-and-history`

- [ ] `images/infinite-canvas/asset-library-01.png`
      Screen: the asset library opened with command-B
      Must show: the folder tree, several dataset cards with thumbnails, and one card menu open
      State: a workspace with real content
      UI language: English
      Size: >= 1200x1000
      Anonymize: generic dataset names

## `infinite-canvas/generate-media`

- [ ] `images/infinite-canvas/bulk-review-01.png`
      Screen: the review dialog opened by bulk submit
      Must show: at least three rows so the per-node breakdown is clear, plus the submit-all button
      State: nodes with differing reference counts so the value of reviewing is visible
      UI language: English
      Size: >= 1400x1000

## `infinite-canvas/canvas-management`

- [ ] `images/infinite-canvas/canvas-switcher-01.png`
      Screen: the canvas switcher dropdown open from the top bar
      Must show: four or more canvases with meaningful names, the search field, the reorder handles, and the per-row gear
      State: current canvas marked
      UI language: English
      Size: >= 1000x900
      Anonymize: generic canvas names such as Act one, Act two

## `infinite-canvas/overview`

- [ ] `images/infinite-canvas/chrome-01.png`
      Screen: a canvas with a few nodes, nothing selected
      Must show: the whole viewport so all three chrome areas are visible at once
      State: no panels open, zoom near 100%
      UI language: English
      Size: >= 1600x1000
      Anonymize: generic canvas name

## `infinite-canvas/work-with-nodes`

- [ ] `images/infinite-canvas/color-tag-popover-01.png`
      Screen: the color tag popover open from the multi-select toolbar
      Must show: all eight colors, at least three with meaningful names such as approved / needs revision / client-visible
      State: one color currently applied to the selection
      UI language: English
      Size: >= 900x800

## `infinite-canvas/connections-and-references`

- [ ] `images/infinite-canvas/connection-handles-01.png`
      Screen: mid-drag, a connection being pulled from an image node to a generation node
      Must show: both plus handles on the source node, and the in-progress line rendered green to mean the drop is valid
      State: capture mid-gesture; a second frame showing the red invalid state would also be useful
      UI language: English
      Size: >= 1400x900

## `infinite-canvas/create-nodes`

- [ ] `images/infinite-canvas/create-menu-01.png`
      Screen: the double-click create menu open on empty canvas
      Must show: the media type list, the divider, and the Annotation submenu expanded
      State: menu opened over empty space, existing nodes visible behind
      UI language: English
      Size: >= 1000x900

- [ ] `images/infinite-canvas/create-panel-01.png`
      Screen: the create panel opened from the first capsule toolbar icon
      Must show: the full list of node types, and the Annotation grouping
      State: nothing selected on the canvas behind it
      UI language: English
      Size: >= 1000x900

## `infinite-canvas/connections-and-references`

- [ ] `images/infinite-canvas/cut-connection-01.png`
      Screen: a connection clicked so the cut control appears
      Must show: the scissors button at the midpoint of the line, and both connected nodes
      State: one connection selected, others unselected for contrast
      UI language: English
      Size: >= 1200x700

## `infinite-canvas/director-bar`

- [ ] `images/infinite-canvas/director-bar-01.png`
      Screen: the director bar open with a working timeline
      Must show: five or more clips of differing lengths, the playhead, playback controls, snap and loop toggles, and the export control
      State: mid-timeline, one clip selected
      UI language: English
      Size: >= 1800x600, cropped to the bar plus a little canvas above

- [ ] `images/infinite-canvas/director-export-01.png`
      Screen: the director bar export menu open
      Must show: all three options
      State: a timeline with content behind the menu
      UI language: English
      Size: >= 900x600

## `infinite-canvas/generate-media`

- [ ] `images/infinite-canvas/generate-panel-01.png`
      Screen: a generation node selected with its panel open
      Must show: prompt box with placeholder or short prompt, model selector, parameters icon, reference list, and the send button carrying a credit estimate
      State: two or three references connected
      UI language: English
      Size: >= 1400x1000

- [ ] `images/infinite-canvas/generate-params-01.png`
      Screen: the generation parameters popover open from the generate panel
      Must show: aspect ratio choices, batch size, and at least two model specific fields such as seed and resolution
      State: an image model selected
      UI language: English
      Size: >= 1000x900

## `infinite-canvas/keyboard-shortcuts`

- [ ] `images/infinite-canvas/hotkey-help-01.png`
      Screen: the shortcut help popup opened from the question mark in the capsule toolbar
      Must show: all four columns, with key symbols legible
      State: opened on a Mac so the command symbols match the documentation
      UI language: English
      Size: >= 1600x1000

## `infinite-canvas/work-with-nodes`

- [ ] `images/infinite-canvas/icons/add-reference.png`
      Screen: close crop of the add reference button in the multi-select toolbar
      Must show: the icon alone, square crop, transparent or matching background
      State: the button in its normal, enabled state
      UI language: English
      Size: 64x64, exported at 2x so it stays sharp

- [ ] `images/infinite-canvas/icons/align.png`
      Screen: close crop of the align button in the multi-select toolbar
      Must show: the icon alone, square crop, transparent or matching background
      State: the button in its normal, enabled state
      UI language: English
      Size: 64x64, exported at 2x so it stays sharp

- [ ] `images/infinite-canvas/icons/color-tag.png`
      Screen: close crop of the color tag button in the multi-select toolbar
      Must show: the icon alone, square crop, transparent or matching background
      State: the button in its normal, enabled state
      UI language: English
      Size: 64x64, exported at 2x so it stays sharp

- [ ] `images/infinite-canvas/icons/download-selected.png`
      Screen: close crop of the download selected button in the multi-select toolbar
      Must show: the icon alone, square crop, transparent or matching background
      State: the button in its normal, enabled state
      UI language: English
      Size: 64x64, exported at 2x so it stays sharp

- [ ] `images/infinite-canvas/icons/group.png`
      Screen: close crop of the group button in the multi-select toolbar
      Must show: the icon alone, square crop, transparent or matching background
      State: the button in its normal, enabled state
      UI language: English
      Size: 64x64, exported at 2x so it stays sharp

- [ ] `images/infinite-canvas/icons/improve-prompt.png`
      Screen: close crop of the improve prompt button in the multi-select toolbar
      Must show: the icon alone, square crop, transparent or matching background
      State: the button in its normal, enabled state
      UI language: English
      Size: 64x64, exported at 2x so it stays sharp

- [ ] `images/infinite-canvas/icons/model-settings.png`
      Screen: close crop of the model settings button in the multi-select toolbar
      Must show: the icon alone, square crop, transparent or matching background
      State: the button in its normal, enabled state
      UI language: English
      Size: 64x64, exported at 2x so it stays sharp

- [ ] `images/infinite-canvas/icons/submit.png`
      Screen: close crop of the submit button in the multi-select toolbar
      Must show: the icon alone, square crop, transparent or matching background
      State: the button in its normal, enabled state
      UI language: English
      Size: 64x64, exported at 2x so it stays sharp

## `infinite-canvas/navigate-the-canvas`

- [ ] `images/infinite-canvas/minimap-01.png`
      Screen: the minimap open, cropped to itself plus a little surrounding canvas
      Must show: at least three different tag colors so the color mapping is obvious
      State: a canvas with 15 or more nodes so the map has something to show
      UI language: English
      Size: >= 800x600
      Anonymize: none needed

## `infinite-canvas/work-with-nodes`

- [ ] `images/infinite-canvas/multi-select-toolbar-01.png`
      Screen: the multi-select toolbar as it slides up from the bottom
      Must show: every button in the bar, and the option-key fan-out badge on the right
      State: three or more generation nodes selected so all buttons are enabled
      UI language: English
      Size: >= 1400x400, cropped to the toolbar plus a little canvas above it

## `infinite-canvas/node-types`

- [ ] `images/infinite-canvas/node-gallery-01.png`
      Screen: a canvas laid out as a reference sheet of node types
      Must show: at least image, video, text, table, dataset, group, and one annotation node, each with its name label visible
      State: nothing selected, arranged in a readable grid
      UI language: English
      Size: >= 1800x1100
      Anonymize: no real people in the images

## `infinite-canvas/asset-library-and-history`

- [ ] `images/infinite-canvas/node-history-picker-01.png`
      Screen: the history picker open on a media node
      Must show: four or more results so comparison is the obvious use, and a clear indication of which is current
      State: results that differ visibly from one another
      UI language: English
      Size: >= 1200x900
      Anonymize: no real people

## `infinite-canvas/overview`

- [ ] `images/infinite-canvas/overview-01.png`
      Screen: a canvas with a small working graph
      Must show: two image nodes and one text node connected into a generation node, the capsule
      toolbar on the left, the top bar with canvas switcher, and the viewport control bar
      bottom left
      State: zoom near 100%, nothing selected
      UI language: English
      Size: >= 1600x1000
      Anonymize: generic canvas name, no real people in images

## `infinite-canvas/image-presets-and-storyboard`

- [ ] `images/infinite-canvas/preset-menu-01.png`
      Screen: the storyboard tools or generation presets menu open on an image node
      Must show: every preset entry, including all three grid sizes for both storyboard and multi-angle
      State: an image node with media selected
      UI language: English
      Size: >= 1100x900

## `infinite-canvas/connections-and-references`

- [ ] `images/infinite-canvas/reference-list-01.png`
      Screen: the reference list inside an open generate panel
      Must show: at least three references with distinct names, the counter reading something like 3 / 5, and one text reference with its view full text control
      State: a mix of image and text references so both rendering styles appear
      UI language: English
      Size: >= 1200x900

## `infinite-canvas/autosave-and-undo`

- [ ] `images/infinite-canvas/save-status-01.png`
      Screen: close crop of the save status capsule, top left of the canvas
      Must show: the capsule clearly. Capture the saving and saved states as two frames if you can, and use the saved one here
      State: after an edit has been written
      UI language: English
      Size: >= 700x200, cropped tight

## `infinite-canvas/video-tools`

- [ ] `images/infinite-canvas/selective-edit-01.png`
      Screen: a video node after a selective regeneration has completed
      Must show: the four preview options, and the stitch control
      State: a finished selective edit, not one still running
      UI language: English
      Size: >= 1200x900

## `infinite-canvas/image-presets-and-storyboard`

- [ ] `images/infinite-canvas/storyboard-builder-01.png`
      Screen: the storyboard builder open from an image or dataset node
      Must show: the frame filter, the pool of frames, and four or more frames dragged into the selection in order
      State: mid-assembly so both panes have content
      UI language: English
      Size: >= 1600x1000
      Anonymize: no real people

## `infinite-canvas/text-table-and-youtube-nodes`

- [ ] `images/infinite-canvas/table-field-mapping-01.png`
      Screen: a table node holding a short shot list, with the field mapping control open
      Must show: at least four rows of real-looking shot data, and the prompt column clearly mapped
      State: mapping panel open over the table
      UI language: English
      Size: >= 1400x900

## `infinite-canvas/canvas-management`

- [ ] `images/infinite-canvas/tag-filter-01.png`
      Screen: the color tag filter dropdown open from the left of the top bar
      Must show: the All entry plus several named tags with node counts, and the download-this-tag action revealed on hover
      State: tags named as workflow states
      UI language: English
      Size: >= 900x800

## `infinite-canvas/text-table-and-youtube-nodes`

- [ ] `images/infinite-canvas/text-node-toolbar-01.png`
      Screen: a selected text node with real prose in it
      Must show: the full node toolbar with every icon, and enough body text that the Markdown rendering is visible
      State: node selected, not in edit mode
      UI language: English
      Size: >= 1200x900

## `infinite-canvas/video-tools`

- [ ] `images/infinite-canvas/video-node-toolbar-01.png`
      Screen: a selected video node showing a frame of its clip
      Must show: the complete node toolbar with every icon legible
      State: node selected, video paused on a recognisable frame
      UI language: English
      Size: >= 1200x900
      Anonymize: no real people in the clip

## `infinite-canvas/navigate-the-canvas`

- [ ] `images/infinite-canvas/viewport-bar-01.png`
      Screen: close crop of the viewport control bar, bottom left of the canvas
      Must show: every button, and the zoom percentage reading a non-round value such as 87%
      State: standard performance mode, minimap off
      UI language: English
      Size: >= 900x220, cropped tight
      Anonymize: none needed

## `organizations/roles-and-permissions`

- [ ] `images/organizations/console-members-01.png`
      Screen: the Members and limits tab of the organization console
      Must show: several members with different roles, at least one with no limit configured, and one row menu open showing transfer owner
      State: signed in as the owner so transfer owner appears
      UI language: English
      Size: >= 1800x1000
      Anonymize: generic member names and avatars

## `organizations/overview`

- [ ] `images/organizations/create-org-01.png`
      Screen: step two of the create organization wizard
      Must show: the owner limit fields and the default member limit template
      State: at least one owner limit filled so the requirement is illustrated
      UI language: English
      Size: >= 1100x900

## `organizations/invite-members`

- [ ] `images/organizations/invite-block-01.png`
      Screen: the invite block at the top of the Members and limits tab
      Must show: every field and the generate invite link button
      State: role set to member, limits left blank so the default template note applies
      UI language: English
      Size: >= 1400x600

## `workspaces/settings`

- [ ] `images/workspaces/app-management-01.png`
      Screen: the App management tab in workspace settings, with the add app dialog open over it
      Must show: the existing apps behind, and both addable apps in the dialog
      State: signed in with permission to manage the workspace
      UI language: English
      Size: >= 1400x1000

## `workspaces/create-a-workspace`

- [ ] `images/workspaces/create-01.png`
      Screen: the Create workspace dialog opened from the workspace list
      Must show: name field, both primary image generation mode cards, the expanded settings section
      with dialogue language / aspect ratio / default image model / default video model,
      and both footer buttons
      State: personal context, so the billing mode selector is absent; canvas mode not selected
      UI language: English
      Size: >= 1400x1000
      Anonymize: generic workspace name

## `workspaces/members-and-permissions`

- [ ] `images/workspaces/invite-link-01.png`
      Screen: the Members tab with an active invite link
      Must show: the member list with mixed roles, the invite link with its expiry time, and the copy and revoke controls
      State: signed in as the owner so the role controls are enabled
      UI language: English
      Size: >= 1400x1000
      Anonymize: generic member names and avatars

## `workspaces/settings`

- [ ] `images/workspaces/settings-01.png`
      Screen: the workspace settings dialog, Basic settings tab active
      Must show: all eight tab labels down the left, and the basic pane with avatar, name,
      description, aspect ratio, dialogue language, default app, and default models
      State: signed in as the workspace owner so every tab is visible; danger zone out of frame
      UI language: English
      Size: >= 1400x1000
      Anonymize: generic workspace name and description

## `workspaces/viewer-access`

- [ ] `images/workspaces/viewer-permissions-01.png`
      Screen: the visible canvases section of the viewer permissions panel
      Must show: several canvases listed with checkboxes, the color tag selection with named tags, and the annotation toggle
      State: some canvases and tags unticked so the granularity is visible
      UI language: English
      Size: >= 1200x1000

## `workspaces/overview`

- [ ] `images/workspaces/workspace-list-01.png`
      Screen: My Workspaces
      Must show: the four scope filters, the sort control, the search box, the create button, and at least four cards with covers and member avatars
      State: one card favorited so the star state is visible
      UI language: English
      Size: >= 1600x1000
      Anonymize: generic workspace names, generic avatars
